#include "../includes/miniRT.h"

int main(int argc, char **argv)
{
	t_data	*data;

	data = malloc(sizeof(t_data));
	// if (!data)
		//exit with code -1
	// input_parser(data);
	printf("done!\n");
	return (0);
}
